import { Field, Item } from 'primitives';
import styled from 'styled-components';

export const Container = styled.div``;

export const ItemSelect = styled(Item)`
  padding-left: 1.6rem;
`;

export const FieldSelect = styled(Field)`
  gap: 0.8rem;
`;
