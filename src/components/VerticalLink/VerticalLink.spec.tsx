import { render } from '@testing-library/react';
import { describe, expect, it } from 'vitest';

import { VerticalLink } from './VerticalLink';

describe('<VerticalLink />', () => {
  it('should render the component', () => {
    render(<VerticalLink />);

    expect(false).toBeTruthy();
  });
});
