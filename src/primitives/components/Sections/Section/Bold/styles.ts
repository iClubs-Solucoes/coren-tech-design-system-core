import { colors, font } from 'common/styles';
import styled from 'dynamic-styled-components';

export const BoldContainer = styled.b`
  font: ${font.presets.titlesAndLabels};
  color: ${colors.neutral.black4};
`;
